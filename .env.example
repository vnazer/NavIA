# Variables de entorno publicadas al cliente (prefijo EXPO_PUBLIC_).
# Copiá este archivo como .env.local y completá los valores.

# API key de aisstream.io (gratis, registro en https://aisstream.io/).
# Necesario para la capa de tráfico AIS en el mapa.
EXPO_PUBLIC_AISSTREAM_API_KEY=
